from src.lesson import Lesson

lesson = Lesson(
    title="Funciones estándar",
    content=(
        "C++ ofrece muchas funciones listas para usar. Algunas están en <code>&lt;cmath&gt;</code>:\n\n"
        "• <code>sqrt(x)</code>: raíz cuadrada\n"
        "• <code>pow(x, y)</code>: potencia\n"
        "• <code>abs(x)</code>: valor absoluto\n"
        "• <code>round(x)</code>: redondeo\n\n"
        "<b>Ejemplo:</b>\n"
        "<code>double raiz = sqrt(9.0);</code>  // Guarda 3.0"
    )
)
